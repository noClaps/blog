when:
  - event: ["push"]
    branch: ["main"]

engine: "nixery"

dependencies:
  nixpkgs:
    - nodejs
    - go

steps:
  - name: Build the site
    command: make build
  - name: Deploy the site
    command: npx wrangler deploy
