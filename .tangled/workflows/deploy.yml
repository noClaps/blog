when:
  - event: ["push"]
    branch: ["main"]

engine: "nixery"

dependencies:
  nixpkgs:
    - wrangler
    - go

steps:
  - name: Build the site
    command: make build
  - name: Deploy the site
    command: wrangler deploy
