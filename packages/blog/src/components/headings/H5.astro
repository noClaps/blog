---
interface Props {
    id: string;
}

const { id } = Astro.props;
---

<h5 {id}>
    <a href={`#${id}`}><slot /></a>
</h5>

<style>
    h5 {
        scroll-margin-top: 4.5rem;
    }

    a {
        color: var(--text);

        &:hover {
            text-decoration: none;
        }
    }
</style>
