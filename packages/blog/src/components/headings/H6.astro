---
interface Props {
    id: string;
}

const { id } = Astro.props;
---

<h6 {id}>
    <a href={`#${id}`}><slot /></a>
</h6>

<style>
    h6 {
        scroll-margin-top: 4.5rem;
    }

    a {
        color: var(--text);

        &:hover {
            text-decoration: none;
        }
    }
</style>
