<div>
    <section>
        <h1>The Blog of Random</h1>
        <p>A blog about the most random things you can think of.</p>
    </section>
    <nav>
        <a href="/" data-selected={Astro.url.pathname === "/"}>Blog</a> /
        <a href="/notes" data-selected={Astro.url.pathname.startsWith("/notes")}
            >Notes</a
        >
    </nav>
</div>

<style>
    div {
        display: flex;
        justify-content: space-between;

        @media (max-width: 768px) {
            flex-direction: column;
        }
    }

    h1 {
        font-size: 2.5rem;
        margin: 0;
        color: var(--primary);
    }

    p {
        font-size: 1.25rem;
        line-height: 1.25;
    }

    nav {
        font-size: 1.25rem;
    }

    a {
        color: var(--text);
        opacity: 0.5;
        transition: all 0.25s;

        &[data-selected] {
            opacity: 1;
        }

        &:hover {
            color: var(--primary);
            opacity: 1;
            text-decoration: none;
        }
    }
</style>
