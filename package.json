{
  "name": "blog",
  "devDependencies": {
    "@noclaps/znak": "^0.12.6",
    "@types/bun": "^1.1.17",
    "typescript": "^5.7.3"
  },
  "scripts": {
    "build": "bun scripts/build.ts",
    "dev": "bun scripts/dev.ts",
    "preview": "NODE_ENV=production bun run build && bunx serve -n dist"
  },
  "type": "module"
}
