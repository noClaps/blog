{
  "name": "blog",
  "devDependencies": {
    "@noclaps/znak": "^0.12.4",
    "@types/bun": "^1.1.14",
    "typescript": "^5.7.2"
  },
  "scripts": {
    "build": "bun scripts/build.ts",
    "dev": "bun run build && bunx serve -n dist"
  },
  "type": "module"
}
