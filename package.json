{
  "name": "blog",
  "devDependencies": {
    "@noclaps/znak": "^0.12.4",
    "@types/bun": "^1.1.16",
    "typescript": "^5.7.3"
  },
  "scripts": {
    "build": "bun scripts/build.ts",
    "dev": "bun run build && bunx serve -n dist"
  },
  "type": "module"
}
